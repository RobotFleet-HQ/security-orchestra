# billing-api/.dockerignore
# Used when building from the billing-api/ directory directly.
# Note: Railway builds from the repo root — the root .dockerignore takes effect.

node_modules
dist
.env
.env.*
!.env.example
*.db
coverage
.DS_Store
