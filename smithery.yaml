startCommand:
  type: http
  url: https://security-orchestra-orchestrator.onrender.com/mcp
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        title: "Security Orchestra API Key"
        description: "Your API key from https://security-orchestra-billing.onrender.com/signup.html — get a free key instantly"
    required:
      - apiKey
